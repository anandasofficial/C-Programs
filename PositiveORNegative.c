#include <stdio.h>

int main()
{
    int flag = 1;
    while(flag)
    {
        int number;
    printf("Enter an Integer:");
    scanf("%d",&number);

    if(number > 0)
    {
        printf("The Given Integer %d is POSITIVE!\n\n",number);
    }
    else
    {
        printf("The Given Integer %d is NEGATIVE!\n\n",number);
    }

}

    }

